**backend packages** :
npm i express mongoose dotenv cors jsonwebtoken bcryptjs cookie-parser nodemon morgan crypto nodemailer cloudinary multer qrcode google-auth-library

**frontend packages** :
npm i react-router-dom react-icons axios react-hot-toast lucide-react react-spinners @reduxjs/toolkit react-redux @react-oauth/google file-saver react-qr-code html2canvas jspdf moment framer-motion

---

_in instagram we say reels, but in VYBE we say loops for thoda uniqueness_

pino-http pino-pretty

axios.post(url, body, config)

--- --- --- --- **redux** --- --- --- ---

useSelector - for getting data from redux store
(In Javascript we use useSelector & In typescript we use useAppSelector)

useDispatch - for sending data to redux store
(In Javascript we use useDispatch & In typescript we use useAppDispatch)

**Breakpoints (default Tailwind v4 breakpoints for screens):**

sm → ≥640px
md → ≥768px
lg → ≥1024px
xl → ≥1280px
2xl → ≥1536px

=========================================================================

🧠 Simple one-line explanation (yaad rakhna)

includes() object ke andar _id nahi dekhta, sirf pura object compare karta hai

// default: "https://icon-library.com/images/anonymous-avatar-icon/anonymous-avatar-icon-25.jpg",